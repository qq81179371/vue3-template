const state = {
  routes: [
    {
      name: '首页',
      meta: {
        roles: 'admin'
      }
    }
  ]
}
const mutations = {}

const actions = {}

export default {
  namespaced: true,
  state,
  mutations,
  actions
}
